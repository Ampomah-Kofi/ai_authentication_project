# Archive - Do Not Deploy

This folder contains obsolete or historical artifacts retained only for traceability.

- `archive/DEPRECATED_SUPABASE_SCHEMA.sql` is the superseded PostgreSQL/Supabase prototype and must not be used for participant data.
- `archive/INITIAL_HOSTING_REQUEST_EMAIL.md` is historical correspondence, not current deployment guidance.

Current hosting instructions are in `deployment/` and `review/`.
